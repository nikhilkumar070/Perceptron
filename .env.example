# Leave AI_PROVIDER=local for a zero-key demo using the built-in resume intelligence engine.
AI_PROVIDER=local
# Optional: set AI_PROVIDER=openai and provide an API key for deeper LLM analysis.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
